import { LetsTalk } from "../buttons/LetsTalk";
import { Logo } from "./Logo";

export const NavBar = () => {
  return (
    <nav className="navbar">
      <Logo />
      <LetsTalk />
    </nav>
  );
};
